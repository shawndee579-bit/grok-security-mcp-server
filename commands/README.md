# Commands Folder

This folder contains reusable scripts and commands that can be executed through the MCP server.

## Structure
- `shell/` → Shell scripts (.sh)
- `python/` → Python scripts (.py)

## How to use
Grok can list and run these scripts using the terminal tool.

## Adding new commands
Just add your `.sh` or `.py` file in the correct folder and commit.